Developed a Smart Room Booking System with Google Calendar integration using Node.js and Express.js. The system allows users to book rooms dynamically, prevents double bookings through conflict detection logic, and automatically creates events in Google Calendar using OAuth 2.0 authentication. Implemented booking management features including viewing and cancelling bookings with a responsive frontend interface.
